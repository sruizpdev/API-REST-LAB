import * as React from 'react';
import { AppLayout } from 'layouts';
import { PanelContainer } from '../pods/panel';

export const DashboardScene = () => (
  <AppLayout>
    <PanelContainer />
  </AppLayout>
);
